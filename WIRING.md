# 라인트레이서 배선 정리

Arduino UNO · L298N · 적외선 센서 2개 · HC-SR04 · CdS · 액티브 부저

대화에서 사용한 핀 설정을 기준으로 정리했다. 좌우는 **로봇이 전진하는 방향을 기준**으로 한다. 실제 모터드라이버 단자 표기와 현재 스케치의 핀 선언을 함께 확인한다.

## 1. UNO 핀 연결표

| UNO 핀 | 연결 대상 | 용도 |
|---|---|---|
| A3 | 왼쪽 적외선 센서 AO | 왼쪽 반사광 아날로그 입력 |
| A1 | 오른쪽 적외선 센서 AO | 오른쪽 반사광 아날로그 입력 |
| A2 | CdS 조도센서 한쪽 다리 | 내부 풀업을 이용한 조도 입력 |
| D2 | HC-SR04 ECHO | 초음파 수신 신호 |
| D4 | HC-SR04 TRIG | 초음파 발신 신호 |
| D3 | 액티브 부저 + | 부저 제어, 직접 연결 조건은 아래 참조 |
| D5 | L298N ENA | 오른쪽 모터 PWM |
| D7 | L298N IN1 | 오른쪽 모터 방향 |
| D8 | L298N IN2 | 오른쪽 모터 방향 |
| D6 | L298N ENB | 왼쪽 모터 PWM |
| D9 | L298N IN3 | 왼쪽 모터 방향 |
| D10 | L298N IN4 | 왼쪽 모터 방향 |
| 5V | 센서 전원 분배선 | 적외선 2개와 초음파 센서 전원 |
| GND | 공통 GND 분배선 | 센서·부저·드라이버의 기준 전위 |

현재 적외선 입력은 **왼쪽 A3, 오른쪽 A1**이다. 초기 테스트에서 사용한 A0와 혼동하지 않는다.

## 2. 배터리 및 전원 분기

배터리는 7.4V, 2200mAh이며 커넥터에서 UNO와 모터드라이버로 분기한다.

| 출발점 | 연결 지점 |
|---|---|
| 배터리 + | UNO DC 전원잭의 중심 + 단자 |
| 배터리 − | UNO DC 전원잭의 외측 − 단자 |
| 배터리 +의 별도 분기 | L298N `12V` 또는 `VS` 모터 전원 입력 |
| 배터리 −의 별도 분기 | L298N GND |
| UNO GND | 센서 공통 GND 및 L298N과 공통 기준 |
| UNO 5V | 센서용 5V 분배선 |

- L298N의 `12V` 표기는 모터 전원 입력 단자 이름이다. 이 구성에서는 배터리 7.4V를 연결한다.
- 배터리 전압을 UNO **5V 핀이나 센서 VCC에 직접 연결하지 않는다.**
- 현재 전원 분기 구성에서는 **L298N의 5V 단자와 UNO 5V를 연결하지 않는다.**
- L298N의 `5V-EN`은 보드 내장 레귤레이터용 점퍼다. ENA/ENB 속도 제어 점퍼와 다르다. 실제 모듈의 표기와 레귤레이터 사용 조건을 확인해야 하며, 이 문서만으로 현재 장착 상태가 확인된 것은 아니다.
- 모터 전류가 흐르는 전원선과 GND 귀환선은 배터리와 드라이버 사이에 직접 배선한다. 센서용 빵판이나 가는 신호 점퍼선을 경유시키지 않는다.

## 3. 적외선 센서 2개

| 센서 단자 | 왼쪽 센서 | 오른쪽 센서 |
|---|---|---|
| VCC | UNO 5V 분배선 | UNO 5V 분배선 |
| GND | 공통 GND | 공통 GND |
| AO | UNO A3 | UNO A1 |
| DO | 연결하지 않음 | 연결하지 않음 |

센서의 `AO`는 알파벳 O로 표시된 아날로그 출력이다. UNO의 `A3`, `A1`에 연결하고 `analogRead()`로 읽는다. 센서 LED의 전환과 AO의 측정값 변화는 별도로 확인한다.

```cpp
const byte IR_L = A3;
const byte IR_R = A1;
```

흰색·검은색 판정 임계값은 실제 장착 높이와 바닥에서 측정해 조정한다. 두 센서의 원시 ADC 값이 같을 필요는 없다.

## 4. 초음파 센서 HC-SR04

| 센서 단자 | UNO 연결 |
|---|---|
| VCC | 5V |
| TRIG | D4 |
| ECHO | D2 |
| GND | 공통 GND |

전원을 끈 뒤 센서 기판에 인쇄된 단자 이름을 확인한다. 방향만 보고 핀 순서를 추측하지 않는다.

## 5. 조도센서 CdS — 외부 저항 없는 테스트 구성

| CdS 다리 | 연결 |
|---|---|
| 한쪽 다리 | UNO A2 |
| 다른 다리 | 공통 GND |

CdS에는 극성이 없으므로 긴 다리와 짧은 다리를 구분할 필요가 없다. 이 구성에서는 CdS에 별도의 5V 선을 연결하지 않는다. UNO 내부 풀업을 반드시 활성화한다.

```cpp
pinMode(A2, INPUT_PULLUP);
int lightValue = analogRead(A2);
```

이 배선은 밝을수록 값이 낮아지고, 어두울수록 값이 높아지는 방식이다. 내부 풀업의 편차가 있으므로 정밀 조도 측정용이 아니라 밝음/어두움 구분용으로 사용하고 현장에서 기준을 보정한다.

## 6. 2핀 액티브 부저

| 부저 단자 | 기존 테스트 연결 |
|---|---|
| + | UNO D3 |
| − | 공통 GND |

**직접 연결은 5V 구동이 가능하고 소비 전류가 UNO GPIO의 20mA 범위 이내인 부품에 한한다.** 현재 대화만으로 부저의 정격 전류는 확인되지 않았다. 소리가 난다는 사실만으로 직접 구동 조건이 검증되는 것은 아니다. 정격이 불명확하거나 더 큰 전류가 필요하면 트랜지스터 구동 회로를 사용한다.

직접 구동 조건을 만족하는 액티브 부저는 D3 HIGH로 켜고 LOW로 끈다. 주파수를 만드는 `tone()` 없이 ON/OFF 제어할 수 있다.

## 7. 모터드라이버와 모터

| L298N 단자 | 연결 |
|---|---|
| ENA | UNO D5 |
| IN1 | UNO D7 |
| IN2 | UNO D8 |
| OUT1 / OUT2 | 오른쪽 모터 두 단자 |
| ENB | UNO D6 |
| IN3 | UNO D9 |
| IN4 | UNO D10 |
| OUT3 / OUT4 | 왼쪽 모터 두 단자 |

PWM 속도 제어를 위해 **ENA·ENB 점퍼를 제거**하고 해당 핀에 UNO 신호선을 연결한다. `5V-EN` 레귤레이터 점퍼와 혼동하지 않는다.

대화에서 확인한 방향 설정은 다음과 같다.

```cpp
const byte EN_L = 6, L1 = 9, L2 = 10;
const byte EN_R = 5, R1 = 7, R2 = 8;

const bool REVERSE_L = true;
const bool REVERSE_R = false;
```

모터 두 선의 순서를 바꾸면 회전 방향도 바뀐다. 배선을 바꾼 뒤에는 바퀴를 띄우고 좌우 전진 방향을 다시 확인한다. 왼쪽 모터가 상대적으로 약해 왼쪽 PWM을 오른쪽보다 10 높이는 보정을 사용했으며, 이는 배선이 아니라 코드 설정이다.

## 8. 공통 GND와 만능기판

- UNO GND, 적외선 센서 2개 GND, 초음파 GND, CdS 한쪽 다리, 부저 −를 공통 GND로 연결한다.
- L298N GND도 UNO와 공통이어야 한다. 모터의 큰 전류는 배터리로 직접 돌아가도록 구성한다.
- 빵판의 파란색 전원 레일 두 줄을 쓰면 두 줄 사이를 점퍼선으로 연결한다. 레일이 중간에서 끊긴 제품은 끊긴 구간도 연결해야 한다.
- 일반 만능기판은 옆 구멍과 자동으로 연결되지 않는다. 실제 동박 패턴을 확인하고 필요한 구간을 전선이나 납땜으로 연결한다.
- 5V 분배선과 GND 분배선을 분리하고, 노출 접합부는 절연한다. 센서 신호선끼리도 납으로 이어지지 않도록 확인한다.

## 9. 전원 투입 전 점검

1. 배터리와 USB를 모두 분리한다.
2. 멀티미터 도통 모드로 각 신호선의 양 끝이 연결되는지 확인한다.
3. 5V 분배선과 각 센서 VCC, GND 분배선과 각 GND의 연결을 확인한다.
4. 5V와 GND 사이에 지속적인 저저항 단락이 없는지 확인한다. 연결된 회로의 커패시터 때문에 일시적으로 소리가 날 수 있으므로 삐 소리만으로 단정하지 않는다.
5. ENA/ENB 점퍼, 전원 극성, 납땜 접합부와 전선 고정을 확인한다.
6. 센서 값을 확인한 뒤 바퀴를 띄워 모터를 테스트하고, 마지막으로 바닥에서 주행한다.

## 10. 배선과 주행 코드의 구분

이 문서는 전체 하드웨어 연결표다. **센서가 연결되어 있어도 코드에서 해당 기능을 사용해야 작동한다.** 최근 단순 주행 테스트는 적외선 센서 중심으로 진행했으므로, 초음파 장애물 정지·조도 정지·부저 알림이 모든 테스트 스케치에 들어 있다고 가정하지 않는다.

코드를 교체할 때는 특히 왼쪽 적외선 `A3`, 오른쪽 `A1`, 조도 `A2`, 왼쪽 모터 방향 반전 설정이 유지되는지 확인한다.
